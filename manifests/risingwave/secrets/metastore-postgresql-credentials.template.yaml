apiVersion: v1
kind: Secret
metadata:
  name: ${BENCHMARK_RISINGWAVE_NAME}-metastore-postgresql-credentials
  namespace: ${BENCHMARK_NAMESPACE}
stringData:
  username: "${BENCHMARK_POSTGRESQL_METASTORE_USERNAME}"
  password: "${BENCHMARK_POSTGRESQL_METASTORE_PASSWORD}"
